services:
  catalog-service:
    build:
      context: ./services/catalog-service
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health | grep -q OK"]
      interval: 5s
      timeout: 2s
      retries: 15
