package com.example.marketplace.catalog.auth;

/**
 * Роли доступа: USER — покупатель (заказы); SELLER — продавец (товары, промокоды); ADMIN — полный доступ.
 */
public enum Role {
    USER,
    SELLER,
    ADMIN
}
